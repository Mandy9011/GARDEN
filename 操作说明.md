# 作业小花园 · 小白操作说明

这是一个**专门给孩子用的 iPad 网页小应用**，用来激励孩子每天完成作业。
全部数据都存在 **iPad 本地**，不需要联网、不需要服务器，关掉也不会丢。

---

## 一、iPad 如何把网页添加到主屏幕当 App（全屏使用）

1. 用 **Safari 浏览器**打开这个网页（需要先通过电脑把这个文件夹里的 `index.html` 部署到一个网址，或用本地服务器/网盘生成链接后在 iPad Safari 打开）。
2. 点击 Safari 底部工具栏的 **「分享」按钮**（一个方块带向上箭头）。
3. 在分享菜单里向下滑，找到并点击 **「添加到主屏幕」**。
4. 可以给它起个名字（默认“作业小花园”），点右上角 **「添加」**。
5. 回到 iPad 主屏幕，会发现多了一个图标，**点它就能像普通 App 一样全屏打开**，没有浏览器地址栏。

> 小提示：第一次打开后建议联网一次，让 iPad 把应用缓存好，之后离线也能用。

---

## 二、家长怎么手动设置每天 4 个作业密码

应用里有 **4 项固定作业**：语文、数学、英语、体育。家长每天生成 4 个密码，通过微信发给孩子，孩子完成一项就输入对应密码领生命值。

设置步骤：

1. 在应用右上角点 **⚙️（齿轮）** 图标。
2. 会要求输入**家长码**（默认是 `1234`），输入后进入家长设置。
3. 在 4 个输入框里分别填写今天的语文 / 数学 / 英语 / 体育密码（比如 `yuwen1`、`shuxue1`……）。
4. 点 **「保存」**。

之后把 4 个密码通过微信发给孩子即可。
**建议每天更换密码**，防止孩子重复使用旧密码刷分。

> 家长码用于保护设置不被孩子误改。如果想改家长码，在家长设置最下方“修改家长码”里填一个新的即可。

### 计分规则（以 iPad 本地时间为准，可在家长设置里改）

默认的计分时段如下（这只是默认值，家长可以改，见下方「修改时间→生命值」）：

| 输入时间 | 一个密码得分 |
|---|---|
| 19:00 之前 | +3 |
| 19:00–20:00 | +2 |
| 20:00–21:00 | +1 |
| 21:00–22:00 | +0 |
| 22:00 之后 / 当天完全没输入 | **−1** |

第二天自动结算：昨天没收到的密码会扣 1 点生命值。

### 🧪 测试模式（家长设置里的新开关）

应用默认处于**测试模式**：孩子可以反复给同一科打卡、同一株花也可以一天浇多次水，方便你先试清楚整个流程。

等测试满意后，打开 **⚙️ → 家长码 → 家长设置**，把「🧪 测试模式」的开关**取消勾选**并保存，就会恢复正常限制：
- 每科作业**每天只能打卡 1 次**；
- 每株植物**每天只能浇水 1 次**（且播种当天不能浇水，从第二天起）。

### ⏰ 修改「时间 → 生命值」规则（家长设置里的新功能）

在家长设置里新增了「⏰ 完成作业时间 → 生命值」编辑区：
- 每一行是一个时间段：`到 <小时> 点 → <得分> 分`。
- 例如「到 19 点 → 3 分」表示 19:00 之前完成得 3 分。
- 点「➕ 添加时间段」可以加更多档；每行右侧 ✕ 可删除。
- 建议把最后一档设为「到 24 点 → −1 分」作为兜底（即最晚时段扣分）。
- 保存后立即生效，作业页顶部提示也会自动更新成你设置的规则。

> 小花旦提示：种子列表里「桃花」已换成「🌷 荷花」（图片键 `lotus`），其余 9 种不变。

---

## 三、怎么用（给孩子看的部分）

- **📚 作业打卡**：完成一项作业后，点 **🎤 语音输入**对着 iPad 说出密码，或手动输入密码，再点 **✅ 验证**。验证成功就获得生命值。
- **🛒 种子商店**：用生命值播种喜欢的花（每颗 5 点）。
- **🌷 我的花园**：从**播种的第二天**起，每天可以花 1 点生命值给植物“浇水”让它长大。长到 **10 点就开花**，永久留在花园里。

### 花园枯萎规则
- 已播种但还没开花的植物，**连续 3 天没浇水 → 枯萎**。
- 枯萎后再 **3 天 → 彻底消失**。
- 所以每天都要来花园照顾一下植物哦！

---

## 四、使用注意事项（很重要）

1. ⚠️ **不要随意修改 iPad 的系统时间！**
   所有计分、扣分、浇水、枯萎都按 iPad 本地时间计算。改了时间会打乱计时逻辑（比如提前/延后触发扣分或枯萎），数据可能错乱。
2. 🎤 **语音输入需要麦克风权限**：第一次用语音时，iPad 会弹出“是否允许麦克风”，点**允许**。如果被拒绝，去 **设置 → Safari → 麦克风** 打开，或直接用手动输入密码。
3. 🔋 建议在 iPad 上把“屏幕使用时间”等对该应用的限制关掉，保证能正常语音。
4. 💾 数据存在本机浏览器里，**清除 Safari 网站数据 / 卸载重装会丢失进度**，重要进度可以截图保存。
5. 🌐 语音识别需要联网（Safari 用苹果服务器识别中文），手动输入则完全离线可用。

---

## 五、文件清单（给想自己部署的家长）

| 文件 | 作用 |
|---|---|
| `index.html` | 应用主页面 |
| `styles.css` | 卡通柔和样式 |
| `app.js` | 全部业务逻辑（计分、花园、语音、存储） |
| `manifest.webmanifest` | PWA 配置（全屏、图标） |
| `sw.js` | 离线缓存（Service Worker） |
| `icon-180.png` / `icon-192.png` / `icon-512.png` | 主屏幕图标 |
| `make_icons.py` | 生成图标的脚本（一般不用动） |
| `images/` | 存放每种花、每个生长阶段的图片（**你可以把自己的图放进来**） |
| `make_sample_images.py` | 生成示例生长图的脚本（一般不用动，想重画示例图可运行） |

部署方式（任选其一）：
- 把整个文件夹放到任意支持 HTTPS 的静态网站空间，用 iPad Safari 打开后“添加到主屏幕”。
- 或在电脑上运行 `python -m http.server` 起一个本地服务器，iPad 与电脑同一局域网访问（仅适合临时体验，重启电脑后链接会变）。

---

## 六、给花园换上自己的花图（可选）

应用自带的 `images/` 文件夹里已经放了一套**示例生长图**（每个生长阶段一张，会自动变大/开花）。如果你想用自己画或拍的花图，按下面规则替换即可：

1. 打开项目里的 `images/` 文件夹，里面有详细的 `README.md` 说明。
2. **默认不需要任何图片**：App 现在会自动画出每株花的生长过程（纯代码绘制，零文件、零存储）。不传图也完全正常。
3. 想换成真实照片时，按命名放图进来即可（照片会盖在自动生成的图上面）：
   - 命名规则：`<图片键>_g<生长值>.<jpg/png/webp>`
   - `<图片键>` 每种花固定（樱花 `sakura`、荷花 `lotus`、茉莉花 `jasmine`、向日葵 `sunflower`、铃兰花 `lilyofvalley`、郁金香 `tulip`、腊梅花 `wintersweet`、玫瑰花 `rose`、牡丹花 `peony`、四叶草 `clover`）。
   - `<生长值>` 是 0~10（0=刚播种，10=盛开），例如荷花的第 3 阶段：`lotus_g3.jpg`、盛开：`lotus_g10.jpg`。
   - 扩展名支持 **jpg / png / webp**，App 自动尝试这三种，**照片用 JPG 最省空间**（压到几十 KB 即可）。
   - 枯萎：`lotus_withered.jpg`；消失：`lotus_disappeared.jpg`。
4. 把做好的图按名字放进 `images/`。**某个阶段没放图，就显示自动生成的图**，不受影响。

> 提示：之前的一批示例 PNG 已删除（它们会让自动生成图被盖住）。如果你想完全用真实照片，把对应花的各阶段图都放进 `images/` 即可；否则留空，自动生成的图就是默认显示。部署 iPad 时把 `images/` 一起上传。

---

## 七、用手机 / 微信扫码测试

想用手机（比如微信扫一扫）打开测试，需要先把应用部署到一个**公网可访问的网址**，再把它做成二维码：

1. 在本工具里执行「部署到 CloudStudio」之类的部署指令，会得到一个公网网址（形如 `https://xxxx.sh3.agentos-app.net`）。
2. 把该网址生成二维码（本目录里的 `make_qr.py` 或任意在线二维码工具均可），用微信「扫一扫」即可在手机上打开。
3. 手机打开后是网页版，可正常测试打卡、商店、花园等所有功能。

⚠️ 几点手机端注意：
- 微信内置浏览器里**语音输入（🎤）大概率不可用**（Web Speech API 在微信内核受限），请用**键盘手动输入密码**测试；iPad Safari 里语音才正常。
- 若微信提示“在浏览器打开”，点开后也能正常使用；或改用系统相机/扫码 App 扫描。
- 当前部署是临时沙箱网址，长期用建议部署到自己的静态托管（见上）。

---

## 八、我的花园 · 排序与删除（新）

- **默认排列**：未开花的（含枯萎中）排在前面，已开花的排在下面；同组内按「种下时间」倒序（新种下的在前）。新种下或开花时会自动归位到这个规则位置。
- **测试模式（家长设置里开启）**：在「我的花园」里**长按花名**，或**右键点花名**，会弹出确认框可删除该株花（删除不可恢复）。方便测试。
- **关闭测试模式（正式使用）**：花名**长按可以上下拖动**，给花朵排个自己喜欢的顺序；松手即自动保存。此时不能删除（避免孩子误删）。

> 小提示：长按是指手指/鼠标按住花名不松约半秒。拖动排序只在关闭测试模式后可用；在测试模式里长按是“删除”。

---

## 九、正式投入使用：HTML/PWA 还是小程序？

结论先说：**这个应用已经是一个 HTML + PWA 网页应用，针对 iPad 全屏、无后端、数据本地存储的场景，直接用 HTML/PWA 最合适**，几乎不用重写。微信小程序则需要用另一套框架（WXML/WXSS/JS）基本重写，还要注册账号、审核，投入大、收益小。

| 维度 | HTML / PWA（推荐） | 微信小程序 |
| --- | --- | --- |
| 是否需要重写 | 否，现有代码直接用 | 需要整套重写（小程序框架） |
| 后端 / 服务器 | 不需要，localStorage 本地存 | 需要小程序后台或云开发（跨设备才同步） |
| iPad 主屏幕全屏 | 支持（添加到主屏幕） | 不支持“主屏幕图标”，只在微信里打开 |
| 数据存在哪 | 当前这台 iPad 本地 | 微信账号云端（换设备可见） |
| 部署上线 | 传到任意静态托管 + HTTPS | 微信平台注册、提交审核、发布 |
| 适合本场景 | ✅ 单台 iPad、家庭自用 | ❌ 过重，且孩子要在微信里用 |

**正式上线步骤（HTML/PWA）：**
1. 准备一个**域名**（国内服务器还需 ICP 备案）。
2. 选一个静态托管：国外可用 Vercel / Netlify / Cloudflare Pages；国内可用**腾讯云 COS / 阿里云 OSS / 七牛**等对象存储 + CDN。
3. 把本目录（含 `index.html`、`app.js`、`styles.css`、`sw.js`、`manifest.webmanifest`、`images/`、`icon-*.png`）整体上传。
4. 开启 **HTTPS**（PWA 和“添加到主屏幕”全屏都要求 HTTPS）。
5. 在 iPad 用 Safari 打开网址 → 分享 → 添加到主屏幕，即可得到全屏 App。
6. **数据说明**：生命值、花园都只存在这台 iPad 的浏览器里。换 iPad 或清缓存会丢，需要的话可加一个“导出/导入”功能做备份（可后续定制）。

> 现在的 CloudStudio 临时网址仅用于测试，长期请按上面步骤部署到自己的托管。需要我帮你部署到某个具体平台，告诉我账号/方式即可。
